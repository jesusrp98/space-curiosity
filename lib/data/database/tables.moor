CREATE TABLE nasa_images (
    url TEXT NOT NULL PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT,
    copyright TEXT,
    date DATETIME
);

insertImage: INSERT OR REPLACE INTO nasa_images (url, title, description, copyright, date) VALUES (:url, :title, :description, :copyright, :date);
allImages: SELECT * FROM nasa_images ORDER BY date DESC;